<nav id="nav">
  <h3>
    <% site.navigationItems.forEach(function(item) { %>
      <a href="<%= item.url %>" id="prev" class="<%= item.active ? 'is-active' : '' %>"><%= item.name %></a>
    <% }); %>
  </h3>
</nav>
